# Handoff Document -- Branson '26 Dashboard
**Session ended:** 2026-04-26, approximately 1:30 AM
**Prepared for:** Next vacation-coordinator session

---

## What Was Completed Tonight

### Priority 1: Font Bug Fixes (DONE, LIVE on production)
- Removed Star Jedi from `--font-display` in `web/css/themes/star-wars.css`. Reverted to Orbitron. `@font-face` declarations kept intact for future use with a scoped token.
- Bumped 12 reading-copy selectors from 13-15px to 17px in `web/css/components.css`. Added `@media (max-width: 719px)` block setting the same selectors to 18px on mobile.
- Code reviewer caught lazlo removing eyebrow elements from 3 HTML files (unsolicited). All reverted. New mandatory brief rule: "Do not modify any HTML element not explicitly named in this task."

### Priority 2: Staging/Production Split (DONE, LIVE)
- Production: https://alexshultz.github.io/vacation-dashboard-previews/ (family-facing, no untested work after May 8)
- Staging: https://alexshultz.github.io/vacation-dashboard-staging/ (all dev work goes here first)
- Local staging clone: `/Users/alex/code/vacation-dashboard-staging/`
- CLAUDE.md updated with two-target deploy table and staging-first rule

### Earlier tonight: Star Jedi Font Integration (DONE, LIVE)
- `Starjedi.ttf` and `Starjhol.ttf` copied to `web/assets/fonts/star_jedi/`
- `web/css/themes/star-wars.css` updated with `@font-face` declarations and `--font-display` token (now reverted back to Orbitron per Priority 1 fix)
- `web/themes/DESIGN-star-wars.md` updated

### Earlier tonight: help.html Rebuild (DONE, LIVE)
- `web/help.json` created with 11 sections, minimal Markdown in body strings
- `web/help.html` rewritten with runtime fetch + IIFE renderer
- Help link added to `web/profile.html`
- ADR-009 written documenting the architecture decision

---

## Process Gaps Discovered Tonight (CRITICAL -- read before doing anything)

### 1. Tier System Not Being Followed
The tier declaration block was stated once and then dropped. Every multi-file write requires:
```
**T[N] CHECK**
Trigger: [exact observable signal]
Verifier question: [cold question text]
Verifier task: spawning now
```
Then spawn the verifier delegate_task BEFORE any file write. A 2 AM cron job (job ID: d9de71c5a9fe) is running a full tier compliance review and will deposit results at `docs/Tier Review 2026-04-26.md`.

### 2. Prompt-Engineer Skill Not Being Used
Every lazlo task brief must be drafted by the prompt-engineer subagent, not written inline. Load the `prompt-engineer` skill and delegate brief drafting to a focused subagent. Then do an intent check. Then proceed to grill-me. This was skipped on all three lazlo tasks tonight.

Correct workflow:
1. Load `prompt-engineer` skill
2. Load `branson-lazlo-delegation` skill
3. Load `grill-me` skill
4. Delegate brief drafting to prompt-engineer subagent
5. Intent check
6. Generate grill-me questions, answer them, write to `grill-me docs/`, notify Alex
7. Alex reviews (silence = approval)
8. Fire lazlo
9. Validate output
10. Code review (fresh delegate_task)
11. Handback (safety checks, rsync, push, log, notify)

### 3. Grill-Me Three-Call Pattern Not Used
For large tasks, use three separate calls: grill call, answer file, implementation call. Do not combine them.

---

## Current Sequencing Table

| Priority | Task | Target | Status |
|---|---|---|---|
| 1 | Font bugs: Star Jedi removed, reading copy 17px/18px | Today | DONE |
| 2 | Staging/production split | Before May 8 | DONE |
| 3 | Move 25 events to `schedule.json`, wire `event-timeline.html` to read from it | Before May 8 | NEXT |
| 4 | `index.html` shows only "soon" events, button links to full timeline | Before May 8 | Pending |
| 5 | Past events ghost/toggle on `event-timeline.html` | Before May 8 | Pending |
| 6 | Hamburger menu system (hybrid: persistent Home/Activities/Wishlist bottom bar) | Before May 8 | Pending |
| 7 | Tester pass (Ashlyn, Jordan, Mycah) | May 3-7 | Pending |
| 8 | Send family the link | May 8 | Pending |
| 9 | Coordinator entry form (dropdowns, time pickers, travel time) | May 8-15 | Pending |
| 10 | Drag/drop scheduler GUI | Later | Deferred |

---

## Parking Lot (do not forget)
- How to assign attendees to duplicate activity chips (revisit when designing item 10)
- Star Jedi scoped token (`--font-hero` or similar) for h1-only display at 2rem+. Currently benched. Bring back when theme activation review happens.
- WARN item 12 from help.html code review: `<script>` renderer block sits after `</main>` rather than inside it. Cosmetic only, fix on next help.html touch.

---

## Pre-Launch Checklist Status

| Item | Status |
|---|---|
| `help.html` complete | DONE |
| Tester pass (Ashlyn, Jordan, Mycah) | Pending |
| Send family the link | Pending |

---

## Key Architecture Decisions Made Tonight
- **ADR-009:** help.html content sourced from runtime JSON fetch (`web/help.json`)
- **Two deploy targets established:** production (family) and staging (dev)
- **Hamburger menu decided:** Hybrid approach. Hamburger for full menu. Persistent bottom bar for Home, Activities, Wishlist only.
- **Coordinator tool architecture:** Manual JSON editing first, drag/drop GUI later (or never). Display pipeline takes priority over scheduling GUI.

---

## Known Issues (Non-Blocking)
- Help link on profile.html is a plain `<a>` tag with no button styling. Contrast may be poor on some themes. Deferred until hamburger menu replaces it.
- `events.json` does not exist yet. Current event-timeline.html has 25 events hard-coded. Priority 3 moves these to JSON.

---

## Tester Status (Alex reports in channel)
- Ashlyn: Not yet
- Jordan: Not yet
- Mycah: Not yet

---

## Writing Style Rule (mandatory for all prose)
Load the `alex-writing-style` skill before writing any copy. Key rules: active voice, no dash-based pauses (this is a writing quality rule, not a character substitution rule), clean direct sentences, no hedging. Full guide at `~/vaults/Alex/Thoughts/My AI/Alex Writing Style Guide.md`.

---

## Auth Notes for Lazlo Invocations
Terminal sessions use a sandboxed `~`, not `/Users/alex`. Always extract keys with absolute paths:
```bash
ANTHROPIC_API_KEY=$(grep '^ANTHROPIC_API_KEY=' /Users/alex/.hermes/.env | cut -d'=' -f2)
GITHUB_TOKEN=$(grep '^GITHUB_TOKEN=' /Users/alex/.hermes/.env | cut -d'=' -f2)
```
GitHub push sometimes hangs. Run as background. Kill and restart after 10 minutes of no output.
